import type { JSX } from 'react'

export default function App(): JSX.Element {
  return (
    <div className="app-shell">
      <h1>Auteo</h1>
      <p>Subtitle proofreading workbench — skeleton build.</p>
    </div>
  )
}
